---
"@lcode-cli/lcode": patch
---

Rename the product from Kimi Code to lcode across messages, system prompts, and the default data directory (`~/.lcode`, automatically falling back to `~/.kimi-code` when present).